// import { useParams } from "react-router-dom";

function VideoDetails(){
    // const videoId = useParams();
    // console.log(videoId);

    return(
        <>
            <h1>video details component</h1>
        </>
    );
}

export default VideoDetails;